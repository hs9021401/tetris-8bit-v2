# SUCCESS-MULTIPLAYER

「多人對戰模式」已成功開發並集成。

### 已實現功能：

1.  **多人對戰系統**：
    *   支持最多 4 人同時對戰。
    *   實時同步所有玩家的遊戲狀態（網格、分數、遊戲結束狀態）。
2.  **遊戲大廳與房間**：
    *   實現了遊戲大廳邏輯，可以查看現有房間。
    *   支持「建立房間」功能，房主可以啟動遊戲。
    *   玩家可以輸入姓名並加入房間。
3.  **對戰畫面**：
    *   主玩家畫面保持原有的 8-bit 風格。
    *   右側實時顯示其他玩家的縮小比例畫面（Mini-grid），方便觀察對手進度。
4.  **懲罰機制 (Attack System)**：
    *   實現了攻擊系統：一次消除 2 層增加 1 層垃圾層，3 層增加 2 層，4 層（Tetris）增加 4 層。
    *   垃圾層會隨機產生一個空洞，增加對手難度。
    *   垃圾層在方塊鎖定後增加，確保不干擾正在移動的方塊。
5.  **勝負判定**：
    *   **限時模式**：每局遊戲限時 3 分鐘（180 秒）。
    *   **生存判定**：玩家方塊堆滿頂部（Game Over）則出局。
    *   **獲勝條件**：時間結束時，最後存活且分數最高者獲勝；或當其他玩家全部出局時，最後存活者獲勝。

### 技術實現：
*   **後端**：使用 Node.js + Express + Socket.io 搭建實時服務器。
*   **前端**：使用 React + Socket.io-client 實現即時通訊。
*   **代理**：配置 Vite Proxy 解決開發環境下的跨域問題。

### 操作說明：
1. 進入遊戲後選擇 "MULTIPLAYER"。
2. 輸入姓名，創建或加入房間。
3. 房主點擊 "START GAME" 開始對戰。
